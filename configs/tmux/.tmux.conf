set -g mouse on                          # click to switch windows/panes, scroll
set -g base-index 1                      # number windows from 1, not 0
set -g renumber-windows on               # keep window numbers sequential when one closes
set -g status-right '%H:%M'              # show time in status bar
set -g window-status-current-style bold  # bold the active window name
