#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
source "${PWD}/helpers/color_codes.sh"

if [[ "${VERBOSE_MODE:-}" == true ]]; then
  echo -e "${ANSI_GRAY}$@${ANSI_RESET}"
  sleep "${LOG_SLEEP_DURATION:-0}"
fi
